import styles from '../../../styles/portfolioCard.module.scss'

export default function ExperienceTitle() {
	return (
		<div className={styles.ExperienceTitle}>
			<h1 className={styles.title}>
				<i>Exper</i><i className={styles.blue}>ience</i>
			</h1>
		</div>
	)
}
