import styles from "../../styles/skillsTitle.module.css";

const SkillsTitle = () => (
  <div className={styles.container}>
    <h1 className={styles.title}>skill</h1>
  </div>
);

export default SkillsTitle;
